server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FernandoRub13/microservicios-tutorial
          default-label: gateway
          search-paths: config-data
  application:
    name: config-service