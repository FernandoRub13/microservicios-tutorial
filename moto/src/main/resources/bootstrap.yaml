spring:
  application:
    name: moto-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      name: moto-service
management:
  tracing:
    sampling:
      probability: 1.0